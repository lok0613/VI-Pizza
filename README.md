# Installation
```bash
$ sudo npm install -g ionic cordova gulp
$ sudo npm install -g
$ sudo npm install
$ ionic state reset
$ ionic setup sass
$ bower install
```

# Reference
* https://order1.pizzahut.com.hk/tc/order.html